apiVersion: apps/v1
kind: Deployment
metadata:
  name: weaviate-startup-service
  labels:
    app: weaviate-startup-service
    component: hypha-startup-services
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weaviate-startup-service
  template:
    metadata:
      labels:
        app: weaviate-startup-service
        component: hypha-startup-services
    spec:
      serviceAccountName: weaviate-startup-service
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: weaviate-service
          image: ghcr.io/aicell-lab/hypha-startup-services:latest
          imagePullPolicy: Always
          command: ["hypha-startup-services"]
          args:
            - "weaviate"
            - "remote"
            - "--server-url=https://hypha.aicell.io"
            - "--service-id=weaviate-production"
          env:
            - name: HYPHA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: aria-agents-secrets
                  key: ARIA_AGENTS_TOKEN
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "ps aux | grep hypha-startup-services | grep -v grep"
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - "ps aux | grep hypha-startup-services | grep -v grep"
            initialDelaySeconds: 10
            periodSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: weaviate-startup-service
  labels:
    app: weaviate-startup-service
    component: hypha-startup-services
automountServiceAccountToken: true
